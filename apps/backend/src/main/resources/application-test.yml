spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:runcoach;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
